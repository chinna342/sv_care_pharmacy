{
  "name": "sv-care-pharmacy",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev": "npm --prefix frontend run dev"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "nodemailer": "^9.0.5",
    "tailwindcss": "^4.3.3"
  }
}
